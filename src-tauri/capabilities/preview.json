{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "preview",
  "description": "Capability for preview windows",
  "windows": ["mobile-preview", "desktop-preview"],
  "permissions": [
    "core:event:allow-listen"
  ]
}
